const styles = {
  container: {
    fontFamily: "'Pixelify Sans', sans-serif", // EXACT spelling
    fontSize: "24px",
    color: "#222",
    whiteSpace: "nowrap"
  }
};

export default function TypingAnimation() {
  return (
    <div style={styles.container}>
      hello bestie welcome back!
    </div>
  );
}
